server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # next export 결과: /rules → rules.html
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    error_page 404 /404.html;
}
